# Data

Describe here how to obtain the data:

- If synthetic: state that all data is generated by the experiment scripts.
- If downloadable: provide a `download_data.sh` script and usage instructions.
- If large: do NOT commit it to git; host it externally (Zenodo, Google Drive)
  and download via script.
